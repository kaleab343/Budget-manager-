<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Shape system -->
    <style name="Shape.Medium" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Toolbar style -->
    <style name="Widget.Budget.TopAppBar" parent="Widget.Material3.Toolbar">
        <item name="android:layout_height">wrap_content</item>
        <item name="titleCentered">true</item>
    </style>

    <!-- Button styles -->
    <style name="Widget.Budget.Button.Outlined" parent="Widget.Material3.Button.OutlinedButton" />
    <style name="Widget.Budget.Button.Filled" parent="Widget.Material3.Button" />
</resources>
